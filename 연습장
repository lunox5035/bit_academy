#  2부터30까지 소수리스트 뽑아내기
listi=[]
for i in range(2,31):       # 2~30까지 수
    # listi.append(i)
    for e in range(2,i):    #나누는 수, 1과 i가 아닌 수 
        if i==e:            #자기 자신 제외, 문제: 2가 제외됨
            continue        
        elif i%e==0:        #나머지가 0인 경우 제외
            break
        else:               #소수 리스트 작성
            # print(i)
            listi.append(i)
print(set(listi))           #출력








